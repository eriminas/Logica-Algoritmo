Algoritmo "SomaProcedimento2"

Var
   n,m,soma,res: inteiro

procedimento soma (x,y: inteiro)
inicio
   // res é variável global
   res <- x + y
fimprocedimento

Inicio
   n <- 4
   m <- -8
   soma(n,m)
   escreva(res)
Fimalgoritmo