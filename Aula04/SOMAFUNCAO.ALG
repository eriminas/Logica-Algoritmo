Algoritmo "SomaFuncao"

Var
   n,m,soma,res: inteiro

funcao soma: inteiro
var aux: inteiro
inicio
   // n, m e res são variáveis globais
   aux <- n + m
   retorne aux
fimfuncao

Inicio
   n <- 4
   m <- -8
   res <- soma
   escreva(res)
Fimalgoritmo